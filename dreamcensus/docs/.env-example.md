# Database
DATABASE_URL="postgresql://user:password@localhost:5432/dreamcensus?schema=public"

# Optional: Encryption salt for client-side key derivation
# NEXT_PUBLIC_ENCRYPTION_SALT="generate-with-openssl-rand-base64-32"